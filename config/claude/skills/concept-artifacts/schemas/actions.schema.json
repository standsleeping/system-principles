{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Actions",
  "description": "Output of concept-actions. Operations a user or system can perform on the concept.",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Action name (verb). Should be recognizable to users."
      },
      "signature": {
        "type": "string",
        "description": "Function signature with parameters and return type."
      },
      "requires": {
        "type": ["string", "null"],
        "description": "Precondition. When can this action be performed? Null if always valid."
      },
      "effects": {
        "type": "string",
        "description": "Postcondition. What changes after the action?"
      },
      "description": {
        "type": "string",
        "description": "Optional prose description when signature/requires/effects are insufficient."
      }
    },
    "required": ["name", "signature", "requires", "effects"],
    "additionalProperties": false
  },
  "minItems": 1
}
